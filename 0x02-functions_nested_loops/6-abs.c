#include "main.h"

/**
 * print_sign - entry
 *
 * Description - function that prints a-z
 *
 * @x: int x is the integer
 *
 * Return: Always 0 (Success)
 */

int _abs(int n)
{
if (n < 0)
{
return (-n);
}
else
{
return (n);
}
}
