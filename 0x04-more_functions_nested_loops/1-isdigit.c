#include "main.h"

/**
 * _isdigit - entry
 *
 * Description - function that prints a-z
 *
 * @c: int c is the integer
 *
 * Return: Always 0 (Success)
 */

int _isupper(int c)
{
if (c >= '0' && c <= '9')
{
return (1);
}
else
{
return (0);
}
}
